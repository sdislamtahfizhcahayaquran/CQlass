(function(){
  function esc(v){return typeof escapeHtml==='function'?escapeHtml(v):String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
  function icon(name,size=16){const a=`width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"`;const m={save:`<svg ${a}><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2Z"/><path d="M17 21v-8H7v8"/><path d="M7 3v5h8"/></svg>`,camera:`<svg ${a}><path d="M14.5 4 16 7h3a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V9a2 2 0 0 1 2-2h3l1.5-3z"/><circle cx="12" cy="13" r="3"/></svg>`,chart:`<svg ${a}><path d="M3 3v18h18"/><path d="m7 16 4-5 4 3 4-7"/></svg>`,alert:`<svg ${a}><path d="M10.3 2.9 1.8 17a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 2.9a2 2 0 0 0-3.4 0Z"/><path d="M12 9v4"/><path d="M12 17h.01"/></svg>`,tools:`<svg ${a}><path d="M14.7 6.3a4 4 0 0 0-5-5l2.1 2.1-2.8 2.8-2.1-2.1a4 4 0 0 0 5 5l6.8 6.8a2 2 0 0 0 2.8-2.8z"/><path d="m5 19 4-4"/><path d="M3 21l2-2"/></svg>`};return m[name]||'';}
  function injectStyle(){if(document.getElementById('mt-enh-style'))return;const s=document.createElement('style');s.id='mt-enh-style';s.textContent=`
    .mt-required{color:#b44b39;font-size:10px;font-weight:900;margin-left:4px}.mt-help{font-size:10.5px;color:var(--muted);margin-top:5px;line-height:1.5}.mt-opt-grid{display:grid;grid-template-columns:210px 1fr;gap:10px}.mt-opt-control{width:100%;padding:10px 11px;border:1.5px solid var(--border);border-radius:10px;background:#fff;color:var(--text);font:inherit}.mt-optional{font-size:10px;color:var(--muted);font-weight:650}.mt-dash-wrap{margin-top:14px}.mt-dash-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.mt-dash-card{background:#fff;border:1px solid var(--border);border-radius:14px;padding:14px}.mt-dash-title{font-size:12px;font-weight:900;display:flex;gap:7px;align-items:center}.mt-dash-sub{font-size:10px;color:var(--muted);margin:4px 0 10px}.mt-rank-row{display:grid;grid-template-columns:28px 1fr auto;gap:8px;align-items:center;padding:7px 0;border-bottom:1px solid var(--border)}.mt-rank-row:last-child{border-bottom:0}.mt-rank-no{width:24px;height:24px;border-radius:8px;background:#edf7f6;color:var(--primary);display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:900}.mt-rank-name{font-size:11px;font-weight:800}.mt-rank-meta{font-size:9.5px;color:var(--muted);margin-top:2px}.mt-rank-score{font-size:11px;font-weight:900;color:var(--primary);white-space:nowrap}.mt-mini-select{width:100%;padding:8px 9px;border:1px solid var(--border);border-radius:9px;background:#fff;font:inherit;font-size:10.5px;margin-bottom:7px}.mt-facility-counts{display:grid;grid-template-columns:repeat(3,1fr);gap:6px}.mt-facility-kpi{background:#f4f8f7;border-radius:9px;padding:8px;text-align:center}.mt-facility-kpi b{display:block;font-size:17px;color:var(--primary)}.mt-facility-kpi span{font-size:8.8px;color:var(--muted);font-weight:800}.mt-facility-item{padding:7px 0;border-bottom:1px solid var(--border);font-size:10px}.mt-facility-item:last-child{border-bottom:0}.mt-inline-summary{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.mt-inline-pill{padding:5px 8px;border-radius:999px;background:#eef6f5;font-size:9px;font-weight:850;color:#466864}@media(max-width:900px){.mt-dash-grid{grid-template-columns:1fr}.mt-opt-grid{grid-template-columns:1fr}}
  `;document.head.appendChild(s);}
  function patchForm(){
    if(typeof renderAbsensiSupabaseForm!=='function'||window.__mtFormPatched)return;window.__mtFormPatched=true;injectStyle();
    renderAbsensiSupabaseForm=function(res){
      const body=document.getElementById('absensi-body');if(!body)return;
      if(!absensiState.siswa.length){body.innerHTML='<div class="card"><div class="abs-empty">Tidak ada siswa aktif pada kelas ini.</div></div>';return;}
      const editable=res.editable!==false,rekap=rekapAbsensiState();absensiState.fotoStored=Boolean(res.session?.photo_path||res.session?.photo_url);
      const fac=res.session?.facility_status||'',facNote=res.session?.facility_note||'',addNote=res.session?.additional_note||'';
      body.innerHTML=`
        <div class="card"><div style="display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap"><div><div class="card-title" style="margin-bottom:3px">Morning Talk — ${esc(absensiState.kelasNama)}</div><div style="font-size:12px;color:var(--muted)">${esc(absensiState.tanggal)} · ${absensiState.siswa.length} siswa</div><div class="mt-help">Pilih tanggal yang sudah tersimpan untuk melihat atau mengedit kembali data Morning Talk.</div></div><span class="abs-badge">${absensiState.existing?'DATA TERSIMPAN · DAPAT DIPERBARUI':'BELUM TERSIMPAN'}</span></div><div class="abs-summary" id="absensi-summary">${ABSENSI_STATUS.map(st=>`<div class="abs-summary-item"><strong data-abs-count="${st}">${rekap[st]}</strong><span>${st}</span></div>`).join('')}</div></div>
        <div class="card"><div class="card-title">Tema Morning Talk <span class="mt-required">WAJIB</span></div><input type="text" id="tema-input" class="abs-control" placeholder="Contoh: Adab kepada orang tua" value="${esc(absensiState.tema)}" ${editable?'':'disabled'} oninput="absensiState.tema=this.value"><div class="mt-help">Tema singkat yang disampaikan pada sesi Morning Talk.</div></div>
        <div class="card"><div class="card-title">Foto Morning Talk <span class="mt-required">WAJIB</span></div><div class="abs-photo-box" onclick="${editable?"document.getElementById('foto-input').click()":"void(0)"}"><div id="foto-upload-box">${absensiState.fotoStored?'Foto sudah tersimpan · klik untuk mengganti':'Klik untuk memilih 1 foto suasana Morning Talk'}</div></div><input type="file" id="foto-input" accept="image/jpeg,image/png,image/webp" style="display:none" ${editable?'':'disabled'} onchange="handleFotoSelect(event)"><img id="foto-preview" class="abs-photo-preview" ${absensiState.fotoUrl?`src="${esc(absensiState.fotoUrl)}" style="display:block"`:''}><div class="mt-help">JPG, PNG, atau WEBP. Maksimal 4 MB.</div></div>
        <div class="card"><div class="card-title">Kehadiran Siswa <span class="mt-required">WAJIB</span></div><div class="mt-help" style="margin-bottom:9px">Setiap siswa harus memiliki satu status kehadiran.</div><div id="siswa-list">${absensiState.siswa.map(s=>{const nis=s.nis||s.nisn||'-',current=absensiState.status[s.id]||'Hadir';return `<div class="abs-student-row"><div><div class="abs-student-name">${esc(s.name)}</div><div class="abs-student-meta">NIS ${esc(nis)}</div><input class="abs-note ${current==='Terlambat'?'show':''}" id="abs-note-${esc(s.id)}" value="${esc(absensiState.catatan[s.id]||'')}" placeholder="Catatan singkat (opsional)" ${editable?'':'disabled'} oninput="absensiState.catatan['${esc(s.id)}']=this.value"></div><div class="abs-statuses" data-student="${esc(s.id)}">${ABSENSI_STATUS.map(st=>`<button type="button" class="abs-status-btn ${current===st?'selected':''}" ${editable?'':'disabled'} onclick="setStatusSupabase('${esc(s.id)}','${st}',this)">${st}</button>`).join('')}</div></div>`}).join('')}</div></div>
        <div class="card"><div class="card-title">Catatan Tambahan <span class="mt-optional">OPSIONAL</span></div><div class="mt-opt-grid"><div><label style="display:block;font-size:10.5px;font-weight:850;color:var(--muted);margin-bottom:5px">Kondisi Sapras</label><select id="mt-facility-status" class="mt-opt-control" ${editable?'':'disabled'}><option value="">Tidak ada catatan</option><option value="Rusak" ${fac==='Rusak'?'selected':''}>Rusak</option><option value="Hilang" ${fac==='Hilang'?'selected':''}>Hilang</option><option value="Perlu Ditambah" ${fac==='Perlu Ditambah'?'selected':''}>Perlu ditambah</option></select></div><div><label style="display:block;font-size:10.5px;font-weight:850;color:var(--muted);margin-bottom:5px">Catatan Sapras</label><input id="mt-facility-note" class="mt-opt-control" maxlength="250" value="${esc(facNote)}" placeholder="Singkat saja, mis. 1 kipas kelas rusak" ${editable?'':'disabled'}></div></div><div style="margin-top:10px"><label style="display:block;font-size:10.5px;font-weight:850;color:var(--muted);margin-bottom:5px">Catatan Lain</label><textarea id="mt-additional-note" class="textarea-field" maxlength="500" placeholder="Catatan tambahan bila diperlukan" ${editable?'':'disabled'}>${esc(addNote)}</textarea></div><div class="mt-help">Bagian ini hanya pelengkap. Ringkasan sapras ditampilkan secara singkat pada laporan dan dashboard.</div></div>
        <div class="card"><div class="abs-actions">${!editable?`<span style="font-size:12px;color:var(--danger);font-weight:700">${esc(res.edit_message||'Data sudah tidak dapat diedit.')}</span>`:''}${editable?`<button class="btn abs-save-btn" id="submit-absensi-btn" onclick="submitAbsensiSupabase()">${icon('save',16)} ${absensiState.existing?'Perbarui':'Simpan'} Morning Talk</button>`:''}</div></div>`;
    };
    submitAbsensiSupabase=async function(){
      const btn=document.getElementById('submit-absensi-btn'),temaInput=document.getElementById('tema-input');absensiState.tema=(temaInput?.value||absensiState.tema||'').trim();
      if(!absensiState.tema){showToast('Tema Morning Talk wajib diisi.',true);temaInput?.focus();return;}
      if(!absensiState.siswa.length){showToast('Daftar siswa kosong.',true);return;}
      const allSet=absensiState.siswa.every(s=>ABSENSI_STATUS.includes(absensiState.status[s.id]||''));if(!allSet){showToast('Status kehadiran semua siswa wajib diisi.',true);return;}
      if(!absensiState.fotoBase64&&!absensiState.fotoStored&&!absensiState.fotoUrl){showToast('Foto Morning Talk wajib diunggah.',true);document.getElementById('foto-input')?.focus();return;}
      if(btn){btn.disabled=true;btn.innerHTML='<span class="spinner"></span>Menyimpan...';}
      try{const photoPath=await uploadFotoAbsensiJikaAda();const records=absensiState.siswa.map(s=>({student_id:s.id,status:absensiState.status[s.id],note:(absensiState.catatan[s.id]||'').trim()}));await attendanceRequest('save',{class_id:absensiState.kelasId,date:absensiState.tanggal,theme:absensiState.tema,photo_path:photoPath,facility_status:(document.getElementById('mt-facility-status')?.value||'').trim(),facility_note:(document.getElementById('mt-facility-note')?.value||'').trim(),additional_note:(document.getElementById('mt-additional-note')?.value||'').trim(),records},{timeoutMs:45000});if(typeof roleDashboardCache!=='undefined'){roleDashboardCache=null;roleDashboardCacheAt=0;}showToast(absensiState.existing?'Morning Talk berhasil diperbarui':'Morning Talk berhasil disimpan');await loadAbsensiSupabase();}
      catch(err){const map={photo_required:'Foto Morning Talk wajib diunggah.',attendance_incomplete:'Status kehadiran semua siswa wajib diisi.',facility_status_invalid:'Pilihan kondisi sapras tidak valid.'};showToast(map[err?.message]||err?.message||'Data belum berhasil disimpan.',true);}
      finally{if(btn){btn.disabled=false;btn.innerHTML=`${icon('save',16)} ${absensiState.existing?'Perbarui':'Simpan'} Morning Talk`;}}
    };
  }
  function rankRows(rows,field,label){if(!rows?.length)return '<div class="rd-empty">Belum ada data pada periode ini.</div>';return rows.slice(0,5).map((r,i)=>`<div class="mt-rank-row"><div class="mt-rank-no">${i+1}</div><div><div class="mt-rank-name">${esc(r.name||'Siswa')}</div><div class="mt-rank-meta">${esc(r.class_name||'')}</div></div><div class="mt-rank-score">${Number(r[field]||0)} ${label}</div></div>`).join('');}
  let mtDashData=null;
  window.mtDashAbsenceMode='Alfa';
  window.mtDashChangeAbsence=function(v){window.mtDashAbsenceMode=v;const el=document.getElementById('mt-dash-absence-list');if(el&&mtDashData)el.innerHTML=rankRows(mtDashData.absence?.[v]||[],v,'hari');};
  async function loadDashboardInsights(){const box=document.getElementById('mt-dashboard-insights');if(!box)return;try{const d=await attendanceRequest('analytics',{});mtDashData=d;const comp=d.composition||{},f=d.facilities||{counts:{},items:[]};box.innerHTML=`<div class="mt-dash-grid"><div class="mt-dash-card"><div class="mt-dash-title">${icon('chart')} Kehadiran Terbanyak</div><div class="mt-dash-sub">Peringkat berdasarkan jumlah hadir pada bulan berjalan.</div><div class="mt-inline-summary"><span class="mt-inline-pill">Hadir ${Number(comp.Hadir||0)}</span><span class="mt-inline-pill">Terlambat ${Number(comp.Terlambat||0)}</span><span class="mt-inline-pill">Sakit ${Number(comp.Sakit||0)}</span><span class="mt-inline-pill">Izin ${Number(comp.Izin||0)}</span><span class="mt-inline-pill">Alfa ${Number(comp.Alfa||0)}</span></div><div style="margin-top:8px">${rankRows(d.leaderboard||[],'present','hadir')}</div></div><div class="mt-dash-card"><div class="mt-dash-title">${icon('alert')} Info Ketidakhadiran</div><div class="mt-dash-sub">Pilih jenis ketidakhadiran yang ingin dilihat.</div><select class="mt-mini-select" onchange="mtDashChangeAbsence(this.value)"><option value="Alfa">Alfa terbanyak</option><option value="Izin">Izin terbanyak</option><option value="Sakit">Sakit terbanyak</option></select><div id="mt-dash-absence-list">${rankRows(d.absence?.Alfa||[],'Alfa','hari')}</div></div><div class="mt-dash-card"><div class="mt-dash-title">${icon('tools')} Info Sapras</div><div class="mt-dash-sub">Hanya ringkasan catatan yang perlu perhatian.</div><div class="mt-facility-counts"><div class="mt-facility-kpi"><b>${Number(f.counts?.Rusak||0)}</b><span>Rusak</span></div><div class="mt-facility-kpi"><b>${Number(f.counts?.Hilang||0)}</b><span>Hilang</span></div><div class="mt-facility-kpi"><b>${Number(f.counts?.['Perlu Ditambah']||0)}</b><span>Perlu Ditambah</span></div></div>${(f.items||[]).slice(0,3).map(x=>`<div class="mt-facility-item"><b>${esc(x.class_name||'')}</b> · ${esc(x.status||'')}<br><span style="color:var(--muted)">${esc(x.note||'Tanpa catatan tambahan')}</span></div>`).join('')}</div></div>`;}catch(e){box.innerHTML='<div class="rd-empty">Ringkasan kehadiran belum dapat dimuat.</div>';}}
  function patchDashboard(){if(typeof renderRoleDashboard!=='function'||window.__mtDashPatched)return;window.__mtDashPatched=true;const old=renderRoleDashboard;renderRoleDashboard=function(d){old(d);const role=d?.role||currentUser?.role;if(!['walas','kesiswaan','pimpinan'].includes(String(role||'').toLowerCase()))return;const root=document.getElementById('rd-root');if(!root)return;const wrap=document.createElement('div');wrap.id='mt-dashboard-insights';wrap.className='mt-dash-wrap';wrap.innerHTML='<div class="rd-card"><span class="spinner"></span> Memuat ringkasan kehadiran...</div>';root.appendChild(wrap);loadDashboardInsights();const kpis=root.querySelectorAll('.rd-kpi');kpis.forEach(k=>{const span=k.querySelector('span');if(span?.textContent?.trim()==='Kehadiran Hari Ini')span.textContent='Kehadiran Periode Ini';});};}
  function deepLink(){const p=new URLSearchParams(location.search),cid=p.get('mtClass'),date=p.get('mtDate');if(!cid||!date||window.__mtDeepLinkDone)return;const timer=setInterval(()=>{try{if(typeof currentUser==='undefined'||!currentUser||typeof setActiveModule!=='function')return;window.__mtDeepLinkDone=true;clearInterval(timer);setActiveModule('absensi');setTimeout(()=>{const s=document.getElementById('absensi-kelas-select'),dt=document.getElementById('absensi-tanggal');if(dt){dt.value=date;absensiState.tanggal=date;}if(s){s.value=cid;absensiPilihKelas(cid);}history.replaceState({},'',location.pathname);},800);}catch{}},300);setTimeout(()=>clearInterval(timer),12000);}
  function init(){injectStyle();patchForm();patchDashboard();deepLink();if(window.CQNeutralUI)window.CQNeutralUI.clean();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,0));else setTimeout(init,0);setTimeout(init,800);setTimeout(init,1800);
})();