/* CQlass — legacy attendance readiness override retired.
   Live Readiness now follows the same rule as report preview:
   System is the default source; Rekap Editable is required only when explicitly selected.
*/
(function(){
  'use strict';
  if(window.__CQ_ACADEMIC_PTS_ATTENDANCE_FIX__) return;
  window.__CQ_ACADEMIC_PTS_ATTENDANCE_FIX__=true;
})();
